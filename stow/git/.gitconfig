# vim: ts=2:sts=2:sw=2:noet
[include]
	path = ~/.config/delta/themes.gitconfig

[user]
	name = Eric Hopkins
	email = eric.on.tech@gmail.com
	signingkey = CF0FEF5901173907

[alias]
	aci = commit -am
	aliases = config --get-regex alias
	all = add .
	b = branch
	bvv = branch -vv
	ccis = commit -S -m
	changes-in = diff-tree --pretty --no-commit-id --name-status -r
	ci = commit -m
	co = checkout
	cb = branch --show-current
	cob = checkout -b
	d = diff
	down = push origin --delete
	emails = log --oneline --pretty=format:'%ae'
	graph = log --pretty --graph
	hist = log --oneline
	histv = log --oneline --pretty=format:'%C(yellow)%h%Creset %Cred%cn%Creset %s'
	m = merge
	mnff = merge --no-ff
	plog = log --graph --pretty=format:'%Cred%h%Creset - %Cblue%an%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev --date=relative
	poh = push origin HEAD
	pushall = "!git push origin && git push mirror"
	r = rebase
	ri = rebase -i
	s = status -s
	squash = rebase -i HEAD~$@
	st = status
	unci = reset --soft HEAD~1
	uncommit = reset --soft HEAD~1
	unstage = reset
	untrack = rm --cache
	up = push --set-upstream origin
	upcb = !git up $(git cb)
	upnup = !git checkout -b $1 && git up $1
	whoami = config user.email
	who-wrote-this = log --oneline --pretty=format:'%an %ae'

[init]
	defaultBranch = master

[diff]
	algorithm = histogram

[push]
	default = simple
	autoSetupRemote = true

[pull]
	rebase = true

# Remember conflict resolutions; great during rebases
# Same conflict appears on commits 3, 6, 9? Resolve once
[rerere]
  enabled = true

[github]
	user = IN3D

[core]
	editor = nvim
	pager = command -v delta >/dev/null 2>&1 && delta || less -FRX

[delta]
	features = gruvmax-fang
	hyperlinks = true
	navigate = true
	side-by-side = true
	line-numbers = true

[commit]
	gpgsign = true

[tag]
	gpgSign = true

